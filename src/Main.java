//import dimensions;
import java.awt.*;
public class Main { // update main to fix compile
    public static void main(String[] args) {
        Dimension dim = new Dimension(400, 600);
        gameFrame game = new gameFrame(dim);
    }
}
